# dsh-llm-mlx

A DeepSeek Harness plugin for using an OpenAI-compatible MLX-LM server as a
local model provider.

The package is under active development. It will keep model weights, prompts,
and generated content on the local machine and will bind managed servers to
the loopback interface only.

## License

MIT
